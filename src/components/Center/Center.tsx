import { Flexbox } from 'components/Box';
import styled from 'styled-components';

const Center = styled(Flexbox).attrs({
  justifyContent: 'center',
  alignItems: 'center',
})``;

export default Center;
