import { CardHeader } from 'components/ElementCard/ElementCard.styled';
import styled from 'styled-components';

export const ChartCardHeader = styled(CardHeader)`
  cursor: default;
  align-items: flex-start;
`;
