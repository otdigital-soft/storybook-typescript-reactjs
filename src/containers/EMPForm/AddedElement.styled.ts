import FormInputNumber from 'components/FormInputNumber';
import styled from 'styled-components';

export const ElementInput = styled(FormInputNumber)`
  &.ant-form-item {
    margin-bottom: 0;
  }
`;
