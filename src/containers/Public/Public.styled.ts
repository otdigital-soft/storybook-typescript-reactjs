import Result from 'components/Result';
import styled from 'styled-components';

export const Container = styled.div`
  margin: 156px auto 0;
`;

export const PublicResult = styled(Result)`
  && {
    margin-top: 204px;
  }
`;
